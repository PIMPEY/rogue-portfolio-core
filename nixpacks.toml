[phases.setup]
nixPkgs = ["postgresql"]

[phases.build]
cmds = ["cd app-web && npm install", "cd app-web && npx prisma generate", "cd app-web && npm run build"]

[start]
cmd = "cd app-web && node_modules/.bin/next start -p $PORT"

[variables]
NODE_ENV = "production"
PORT = "3000"
