{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bash .railway/build.sh",
    "watchPatterns": ["app-web/**"]
  },
  "deploy": {
    "startCommand": "cd app-web && node_modules/.bin/next start -p $PORT",
    "healthcheckPath": "/api/health"
  }
}
